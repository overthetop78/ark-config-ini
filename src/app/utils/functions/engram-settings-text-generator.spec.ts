import { EngramSettingsTextGenerator } from './engram-settings-text-generator';

describe('EngramSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new EngramSettingsTextGenerator()).toBeTruthy();
  });
});
