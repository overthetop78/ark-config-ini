import { PveSettingsTextGenerator } from './pve-settings-text-generator';

describe('PveSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new PveSettingsTextGenerator()).toBeTruthy();
  });
});
