import { LootSettingsTextGenerator } from './loot-settings-text-generator';

describe('LootSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new LootSettingsTextGenerator()).toBeTruthy();
  });
});
