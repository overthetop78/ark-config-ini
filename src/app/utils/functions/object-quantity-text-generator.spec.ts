import { ObjectQuantityTextGenerator } from './object-quantity-text-generator';

describe('ObjectQuantityTextGenerator', () => {
  it('should create an instance', () => {
    expect(new ObjectQuantityTextGenerator()).toBeTruthy();
  });
});
