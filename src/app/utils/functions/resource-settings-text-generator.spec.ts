import { ResourceSettingsTextGenerator } from './resource-settings-text-generator';

describe('ResourceSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new ResourceSettingsTextGenerator()).toBeTruthy();
  });
});
