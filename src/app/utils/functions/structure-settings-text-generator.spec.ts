import { StructureSettingsTextGenerator } from './structure-settings-text-generator';

describe('StructureSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new StructureSettingsTextGenerator()).toBeTruthy();
  });
});
