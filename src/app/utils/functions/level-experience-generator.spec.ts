import { LevelExperienceGenerator } from './level-experience-generator';

describe('LevelExperienceGenerator', () => {
  it('should create an instance', () => {
    expect(new LevelExperienceGenerator()).toBeTruthy();
  });
});
