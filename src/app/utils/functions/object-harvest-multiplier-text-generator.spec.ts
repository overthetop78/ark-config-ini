import { ObjectHarvestMultiplierTextGenerator } from './object-harvest-multiplier-text-generator';

describe('ObjectHarvestMultiplierTextGenerator', () => {
  it('should create an instance', () => {
    expect(new ObjectHarvestMultiplierTextGenerator()).toBeTruthy();
  });
});
