import { XpSettingsTextGenerator } from './xp-settings-text-generator';

describe('XpSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new XpSettingsTextGenerator()).toBeTruthy();
  });
});
