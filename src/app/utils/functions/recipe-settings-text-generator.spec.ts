import { RecipeSettingsTextGenerator } from './recipe-settings-text-generator';

describe('RecipeSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new RecipeSettingsTextGenerator()).toBeTruthy();
  });
});
