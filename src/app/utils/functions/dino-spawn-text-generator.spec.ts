import { DinoSpawnTextGenerator } from './dino-spawn-text-generator';

describe('DinoSpawnTextGenerator', () => {
  it('should create an instance', () => {
    expect(new DinoSpawnTextGenerator()).toBeTruthy();
  });
});
