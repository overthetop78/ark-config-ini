import { GameplaySettingsTextGenerator } from './gameplay-settings-text-generator';

describe('GameplaySettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new GameplaySettingsTextGenerator()).toBeTruthy();
  });
});
