import { PlayerSettingsTextGenerator } from './player-settings-text-generator';

describe('PlayerSettingsTextGenerator', () => {
  it('should create an instance', () => {
    expect(new PlayerSettingsTextGenerator()).toBeTruthy();
  });
});
